apiVersion: v1
kind: PersistentVolume
metadata:
  name: persistence-volume
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  hostPath: "/var/lib/pv-data"
